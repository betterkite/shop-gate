# ISSUE-P30 Agent 多轮下钻与 BI 交互（进行中）

## 本次推进

本次先补齐最容易造成 Agent 误判的商品级证据：商品下钻不再只看订单和库存，还会读取同一 `dataset_id` 下的行为事件，返回页面浏览量（PV）、收藏、加购、购买和购买转化率。这样 Agent 可以回答“商品被浏览了多少、加购了多少、最后购买了多少”，并在下一轮继续沿渠道、价格带、类目或转化问题下钻。

## 验收证据

- 后端单测：commerce-data pytest `38 passed`，其中包含商品下钻行为漏斗字段和无数据安全口径验证。
- 真实 API：`retail-demo-p28-elasticity-v1 / item:1000261` 返回 `PV=58`、收藏 `4`、加购 `7`、购买 `5`、购买转化率 `8.62%`，并保留订单、库存和下一步问题。
- 真实 API 同时返回当前数据集、维度和值组成的 `context_url`；前端类型检查通过，工作台下钻表已增加中文字段名、购买转化率百分比和“打开当前下钻视图”链接。浏览器验收确认商品阶段第一页的商品链接能打开该上下文，页面显示商品级行为指标。

## 当前边界与下一步

本次不宣称 P30 完成。仍需：

1. 将下钻结果与工作台图表筛选联动，点击渠道、类目或商品后同步更新相关图表；
2. 在 Agent 回答中提供可直接打开的当前数据集、维度和商品/类目上下文链接；
3. 把下一步问题从静态提示升级为可复用的多轮上下文，保留上一轮的 `dataset_id`、维度和筛选条件；
4. 补充真实 DeepSeek 多轮 E2E 与桌面/移动视觉验收。

`check:module-boundaries` 仍有继承的 `src/app/[project_id]/chat/page.tsx` 行数预算问题（4,122 行，超出 4,100 行上限），P30 后续会在拆分聊天页面时一并处理。
